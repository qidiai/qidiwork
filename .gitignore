target/
*.lock
!Cargo.lock
bin/protoc*.exe
bin/protoc-*.zip
bin/protoc-*.tar.gz
reference/
*.phase3
*.bak.phase3
# 备份/损坏文件不入库,历史交给 git 管理
*.bak*

# External tool caches & local run artifacts (not part of the codebase)
.hologram/
hologram_graph.json
hologram_graph_files.json
mem-log/
.codely/
.codely-cli/
_audit_*.ps1
_audit_check.log
CODELY.md

# ---- 本机整理:构建产物与一次性运行结果 ----
qidi.exe
build_log.txt
build_result.txt

# 本机专用配置(含绝对路径),勿提交
.mcp.json

# 本机数据目录
Library/

# protoc 嵌套解压副本(运行 bin/download_protoc.py 重新生成)
bin/bin/
# Media-gen configs (may contain API keys) - defense in depth
tools/media-gen-agent/agnes_config.json
tools/media-gen-agent/agnes_config.json.*


# ---- Tauri / Vue 前端 (qidiwork GUI) ----
gui/node_modules/
gui/dist/
# docx 预览保真度 spike(一次性验证,结论留档 docs/ 工作记录,不入库)
gui/spike-docx/
gui/src-tauri/target/
gui/src-tauri/gen/schemas/
*.log
_build_*.log
_copy_target.log

# 运行时本地配置（可能含 API key），不入库
.qidi/

# k3 审计临时派发脚本(本机一次性工具,不入库)
tools/_k3_audit_dispatch*.py
# 测试 shell 重定向在 Windows 下意外落到仓库的乱码路径文件（hooks.rs 已修复，此为兜底）
CUsers*
*.snap.new

# 根目录一次性渲染产物（不入库）
.portal-tmp.html
