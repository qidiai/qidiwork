[package]
license = "Apache-2.0"
name = "cf-http"
version = "0.1.0"
edition.workspace = true
description = "Shared reqwest HTTP clients and User-Agent construction for the grok CLI."

[dependencies]
cf-telemetry= { path = "../cf-telemetry" }
cf-auth= { workspace = true }
cf-version= { workspace = true }
reqwest = { workspace = true, features = ["blocking"] }
reqwest-middleware = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
cf-sampler= { path = "../cf-sampler" }
cf-workspace= { workspace = true }

[lints]
workspace = true
