[package]
license = "Apache-2.0"
name = "cf-version"
version = "0.2.0-dev"
edition.workspace = true
description = "Lockstepped grok CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
