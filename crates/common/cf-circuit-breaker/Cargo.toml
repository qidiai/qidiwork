[package]
license = "Apache-2.0"
edition.workspace = true
name = "cf-circuit-breaker"
version = "0.1.0"

[features]
# Exposes force_half_open() and other test hooks for cross-crate tests.
test-hooks = []

[dependencies]
log = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
