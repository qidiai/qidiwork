# 审计报告 T1（主审计 · DeepSeek-V4.1-Flash）——原始存档

> 审计时间：2026-09-20 22:48 | 子代理：01a0bf3f-8f98-7581-ac45-e6223af08a50 | 68 次工具调用 | 10.8 分钟 | 全仓库只读

### 发现清单（按严重级排序）

- **[CRITICAL] 仓库工作区存在 20 个乱码临时文件，内含完整环境变量转储与真实明文 API Key**
  证据（根因定位到具体文件行）：
  - 触发点 1：`crates/codegen/cf-pager/src/notifications/hooks.rs:104-110`（测试 `sets_environment_variables`）
    `let out = dir.path().join("env.txt"); let command = format!("printf '...' > {}", out.display());`
  - 触发点 2：`crates/codegen/cf-pager/src/notifications/hooks.rs:137-140`（测试 `omits_session_id_when_none`）
    `let out = dir.path().join("env.txt"); let command = format!("env > {}", out.display());`
  - 执行路径：`hooks.rs:14-16` `let mut cmd = Command::new("sh"); cmd.arg("-c").arg(command);`（Windows 下解析为 Git Bash 的 `sh`）。`out.display()` 产出 Windows 绝对路径 `C:\Users\ASUS\AppData\Local\Temp\.tmpXXXX\env.txt`，被拼进 shell 重定向；`sh` 把 `\` 当转义符吃掉 → 退化为相对文件名，写到 CWD（仓库根 / cf-pager 目录）；`:` 被 MSYS/Windows 层改写为全角 `：` 以成为合法文件名。
  - 证据（文件名+内容）：`git status` 显示 16 个根目录 + 4 个 `crates/codegen/cf-pager/` 下，形如 `C：UsersASUSAppDataLocalTemp.tmpg4ltSPenv.txt`。读取内容与上述测试逐字吻合，且**包含真实密钥**：
    ```
    MOONSHOT_API_KEY=sk-lZKa****(已轮换/失效)
    OPENAI_API_KEY=sk-e909****(已轮换/失效)
    DEEPSEEK_API_KEY=sk-002d****(已轮换/失效)
    ZHIPU_API_KEY=(ZHIPU key 已轮换/失效)
    ARK_API_KEY=ark-369b****(已轮换/失效)
    AGNES_API_KEYS=sk-klLT****(已轮换/失效),...
    ```
    （`.done` 后缀的 4 个为 0 字节，来自同类 `dir.path().join("done")` 写法的重定向。）
  建议：① 立即删除这 20 个文件并**轮换上述所有泄露密钥**；② 修测试——改用 Rust 侧 `std::fs::write` 落盘，或把路径 `replace('\\', "/")` 后再拼命令，或用 `#[cfg(unix)]` 门控依赖 `sh` 的用例；③ 在 `.gitignore` 加 `*.snap.new` 与对应乱码/`env.txt` 模式，防止再次误提交。**注意：每次 `cargo test -p cf-pager` 都会重新生成带最新环境（含密钥）的这类文件，属持续泄露向量。**

- **[HIGH] 8 个未接受的 insta 快照 `.snap.new`；参考快照因 crate 改名而失配（孤儿快照）**
  证据：目录中仅存在旧前缀参考文件 `xai_grok_pager__scrollback__blocks__tool__edit__tests__diff_basic.snap` 等 8 个；新增的 8 个是 `cf_pager__...snap.new`。两者头部差异证明改名：旧 `source: crates/codegen/xai-grok-pager/src/...` vs 新 `source: crates/codegen/cf-pager/src/...`。`crates/codegen/cf-pager/Cargo.toml:2 name = "cf-pager"` → insta 期望前缀 `cf_pager__`，找不到匹配参考 → 每次运行生成 `.snap.new`。无 `cf_pager__*.snap` 参考文件存在。
  建议：`git mv xai_grok_pager__scrollback__blocks__tool__edit__tests__*.snap` 到 `cf_pager__...`（或 `cargo insta accept` 后提交），删除 `.snap.new`，并把 `*.snap.new` 加入 `.gitignore`。

- **[MEDIUM] 认证 scope key 变更会静默作废既有登录态**
  证据：`crates/codegen/cf-shell/src/auth/config.rs:128` `pub const XAI_OAUTH2_ISSUER: &str = "https://api.qidiai.ltd";`（原 `https://auth.x.ai`）；`:283` `client_id: obfstr::obfstr!("qidi-code")`（原 UUID `b1a00492-073a-47ea-816f-4c329264a828`）。scope key = `{issuer}::{client_id}`（`auth/storage.rs:330` `read_token_by_scope` 按此键查 `auth.json`）→ 旧安装的 token 落在旧键下，升级后将报"尚未登录"，用户须重新登录。属品牌化迁移的预期行为，但应作为破坏性变更显式说明/提供迁移。

- **[MEDIUM] 更新链失去独立回退源，收敛为单一自建 origin**
  证据：`crates/codegen/cf-update/src/version.rs:18-26` `CLI_BASE_URL_PRIMARY` 与 `CLI_BASE_URL_FALLBACK` 现同为 `https://qidiwork.qidiai.ltd/cli`（注释：GCS 镜像"self-hosted 暂不可用"）。原先 primary(Cloudflare)/fallback(GCS) 双源异构，现单源；单源被劫持/宕机即不可恢复。另 `auto_update.rs:29-34 manual_install_cmd()` 与 `reinstall_hint` 仍为 `curl|bash` / `irm|iex` 管道直执行。
  建议：确认 `auto_update.rs:1194 download_verified_from_base` 对下载物做强校验（哈希/签名）并保留记录；将管道直执行显式标注为已接受风险。

- **[MEDIUM] 新增 `gui/src-tauri/src/auth.rs` scope 回退可能取到错误 token**
  证据：`gui/src-tauri/src/auth.rs:119-123`
  ```rust
  map.get(&scope_key()).cloned().or_else(|| {
      // 兜底: ... 取唯一非 API key 项。
      map.into_values().find(|a| !a.key.trim().is_empty())
  })
  ```
  注释称"取唯一…项"，但代码取的是**第一个** key 非空项，未校验唯一性；当 `auth.json` 含多个 scope（例如其它 issuer 的旧记录）时，可能拿别的 token 去请求 `{issuer}/v1/user`。
  建议：按 `oidc_issuer`/scope 精确匹配，歧义时返回 `None`。

- **[MEDIUM] `build_release.ps1` 为 UTF-8 无 BOM，在 Windows PowerShell 5.1 下解析失败**
  证据：文件首字节 `35,32,81`（无 BOM）；当前 shell `$PSVersionTable.PSVersion = 5.1.26100.9444`；`build_console.txt` 记录 `build_release.ps1:91 字符: 51 字符串缺少终止符: "` 及 59/76 行 `意外的标记"}"`。用 UTF-8 读取该文件本身合法（字符串均闭合），故失败源于 PS 5.1 按 ANSI 代码页误解码中文导致引号错配。
  建议：以 UTF-8 BOM 保存，或改用 `pwsh`，或脚本内文案保持 ASCII。

- **[LOW] `gui/src-tauri/src/auth.rs` 编译告警（private_interfaces）**
  证据（`cargo check -p qidiwork-gui` 输出）：
  ```
  warning: type `UsageToday` is more private than the item `AuthStatus::usage_today`
    --> src\auth.rs:109:5   note: src\auth.rs:89:1
  warning: type `Quota` is more private than the item `AuthStatus::quota`
    --> src\auth.rs:110:5   note: src\auth.rs:95:1
  ```
  建议：`UsageToday`/`Quota` 改为 `pub`（本就要序列化给前端）。

- **[LOW] access token 以普通 `String` 持有，未做零化**
  证据：`gui/src-tauri/src/auth.rs:83 key: String`（`StoredAuth` 可 `Clone`）。纪律上 token 未下发前端（正确），但未用 `secrecy`/`zeroize` 擦除。非阻断。

- **[LOW] 根目录失败日志/构建日志陈旧且未 ignore，`mine_fail.txt` 具误导性**
  证据：未跟踪文件 `baseline_fail.txt / mine_fail.txt / mine_fail_clean.txt / live_fail.txt / live2_fail.txt / baseline_fail_raw.txt(0 字节) / build_console.txt`。`mine_fail.txt`（mtime 20:20）**早于** 最终修复（`settings_modal.rs` mtime 21:36），其中 8 条失败（`views::settings_modal::tests::*`、`views::modal::doc_picker_tip_tests::*`、`views::dashboard::render::tests::feedback_badge_*`、`app::dispatch::tests::cta_e2e::plugin_cta_catalog_*`）在最终运行 `live2_fail.txt`（21:55）中已消失。**不能拿 `mine_fail.txt` 当验收依据。**
  建议：删除或纳入 `.gitignore`。

- **[LOW] LF/CRLF 大面积告警**
  证据：`git status` 对约 80 个文件告警 `LF will be replaced by CRLF`。`.gitattributes` 仅固定 `crates/codegen/cf-agent/templates/*.md` 与 `prompt_encrypted.rs` 为 CRLF，无全局策略。
  建议：加入 `* text=auto` 等仓库级行尾策略，避免混合行尾与噪声 diff。

- **[INFO] 供应链：新增依赖干净**
  证据：`git diff gui/src-tauri/Cargo.lock | Select-String '^\+name'` 新增 `reqwest, rustls, rustls-pki-types, rustls-webpki, ring, hyper-rustls, tokio-rustls, webpki-roots, quinn(-proto/-udp), lru-slab, rand(_core/_pcg), chacha20, cpufeatures, subtle, untrusted, ryu, serde_urlencoded, zeroize, cfg_aliases, web-time`，全部 `source = "registry+https://github.com/rust-lang/crates.io-index"`（无 git/path 源），均为 `reqwest 0.12` + `rustls-tls` 的规范传递依赖，无同名仿冒。`gui/src-tauri/Cargo.toml` 显式 `reqwest = { version="0.12", default-features=false, features=["rustls-tls","json"] }`。`gui/package.json` 已有 `@tauri-apps/plugin-opener ^2.5.5`，`capabilities/default.json` 含 `opener:allow-open-url`（最小授权）。**未发现供应链风险。**

- **[INFO] 其余维度**
  - 新增 diff 的 `+` 行未引入任何密钥/口令（对 `sk-…`/`Bearer `/`secret`/`password` 的定向检索仅命中品牌化后的 `eprintln!`）。
  - 重命名一致性：`is_first_party_xai_url` → `is_first_party_qidi_url` 已全量替换（定义于 `cf-shell-base/src/util/mod.rs:69`，17 处引用，0 处旧名残留）。
  - `cf-pager/src/app/dispatch/auth.rs:413` 的 `e.starts_with("如需认证")` 与内核 `cf-shell/src/session/acp_session_impl/mcp.rs:415` 的中文消息前缀一致，耦合正确。
  - 新增文件 `gui/src/composables/useAuth.ts` 全文审计：仅透传脱敏 `AuthStatus`，**未出现 token**，符合"token 只在 Rust 侧"纪律。
  - 安全敏感区（`auth/*`、`remote/*`、`dispatch/auth.rs|permissions.rs|billing.rs`）逐行审：除上述 scope 变更与 URL 品牌化外，**未发现明文落盘、日志打印 token、错误信息泄敏、注入或竞态问题**。

### 审计结论表

- **范围一致性: PASS** — `git diff --numstat | Measure-Object -Line` = 106（+1171/-669），与规格书"106 文件"一致；未跟踪 37 项 = 20 乱码临时文件 + 8 `.snap.new` + 2 新源文件 + 7 日志。
- **正确性: PASS（有保留）** — 最终态 `live2_fail.txt`(21:55) 对比 `baseline_fail.txt`：净差异仅 `upsell_non_max_upgrade_url_is_supergrok`→`..._is_portal`（同名测试，前后均失败=既有失败）与 `app::tests::cli_command_name_is_grok`（被本次修复）。**无净新增失败**；`mine_fail.txt` 系陈旧中间产物。保留项：`auth.rs` scope 回退语义与注释不符（见 MEDIUM）。
- **安全性: FAIL** — 工作区存在含真实明文 API Key 的临时文件（CRITICAL），且该文件由 `hooks.rs` 测试每次运行重新生成；密钥须轮换。
- **规范性: PASS（有保留）** — 品牌化/中文化一致、符号改名无残留；但 `auth.rs` 有 2 处 `private_interfaces` 告警、token 未零化（均 LOW）。
- **仓库卫生: FAIL** — 20 个乱码临时文件 + 8 个 `.snap.new` + 7 个根目录日志污染工作区，且均未 `.gitignore`；`build_release.ps1` 无 BOM 在 PS 5.1 下解析失败。
- **验证: PASS（定向）** — `cargo check -p qidiwork-gui`：`Finished dev profile in 1m 07s`，新增 `auth.rs` 编译通过（2 告警）。未跑全量测试（遵守只读/时间预算），测试结论以仓库内 `live2_fail.txt` 为据。

### 总体判定

存在 CRITICAL（明文密钥泄露风险 + 持续再生成）与 HIGH（孤儿快照/未接受快照）阻断项，须返工。

**AUDIT RESULT: FAIL**

返工清单（优先级序）：① 删除 20 个乱码临时文件并轮换密钥 + 修 `hooks.rs` 测试落盘方式（CRITICAL）；② 修快照前缀/接受快照并 ignore `*.snap.new`（HIGH）；③ 处理 scope 回退语义、更新链单源校验、`build_release.ps1` BOM、清理根目录日志（MEDIUM/LOW）。
