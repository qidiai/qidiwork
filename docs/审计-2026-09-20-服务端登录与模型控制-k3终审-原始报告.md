# 终审报告 T3（终审仲裁 · KIMI-K3）——原始存档

> 终审时间：2026-09-20 23:04 | 子代理：01a0bf52-2dae-7142-b4b5-2020499b0492 | 21 次工具调用 | 7.5 分钟 | 全程静态复核（禁 cargo，避让编译会话）

### 现状核对（vs 审计时快照）
- **改动文件数：无变化**。`git diff --stat` 尾行 = `106 files changed, 1171 insertions(+), 669 deletions(-)`，与审计快照完全一致；HEAD 仍为 `20b6d83`，无新提交。
- **乱码临时文件：仍存在，20 个**（根目录 16 + `crates/codegen/cf-pager/` 内 4），与审计时一致。结构：4×~7.4KB env 转储 + 8×74-89B printf 产物 + 4×0B `.done`（根目录），cf-pager 内另有 **1×10123B env 转储** + 2×小文件 + 1×`.done`。
- **`.snap.new`：仍存在，8 个**，全部 `cf_pager__*` 前缀，未跟踪。
- porcelain 总计 147 行 = 106 M + 41 ??（20 乱码 + 8 snap.new + 8 日志 txt + auth.rs + useAuth.ts + 3 审计文档）。**结论：工作区冻结，审计证据全部可复现。**

### 复核结果（逐项 PASS/FAIL/已失效）
- **CRITICAL 密钥泄露： PASS（成立，且比报告更严重）**。5 个大 env 转储（报告只算了根目录 4 个，漏算 cf-pager 内 10123B 的 `tmp…xmvhPMenv.txt`）每个均含恰好 6 行 `(API_KEY|TOKEN|SECRET)=`：键名前缀 `MOON***`/`AI_B***`/`OPEN***`/`ARK_***`/`ZHIP***`/`DEEP***`（掩码验证，5/6 为真实格式）。根因实点验证：`hooks.rs:107-110`（`printf … > {}` 未转义拼接 `out.display()` Windows 路径）与 `hooks.rs:140`（`env > {}` 全量环境转储——即密钥来源），`hooks.rs:14-16` `Command::new("sh")` 机制与报告一致。
- **HIGH 孤儿快照： PASS（成立）**。`cf-pager/Cargo.toml:2` `name = "cf-pager"` → insta 前缀应为 `cf_pager__`；snapshots 目录实测：8 个旧前缀 `xai_grok_pager__*__edit__tests__*.snap`（受跟踪参考）、**0 个**新前缀 `.snap` 参考、8 个未跟踪 `.snap.new`；`edit.rs:1974/2010/2041/2079/2097/2133/2167/2205` 八个命名快照一一对应。抽 3 对（diff_basic/diff_reflow/diff_multiple_hunks_dual）按 `---` 元数据分隔对齐后**正文逐字节一致** → 纯改名失配，8 个测试当前无参考可比。
- **MEDIUM auth.rs 兜底： PASS（成立，行号需修正）**。实际兜底在 `gui/src-tauri/src/auth.rs:119-123`（报告称 103-107，系 AuthStatus 结构体位置）；`:120` 注释称"取唯一"而 `:121-122` `map.into_values().find(|a| !a.key.trim().is_empty())` 按 BTreeMap 序取首个非空项、**不校验 `len()==1`**。下游 `build_status:144` 以该 key 作 bearer 调 `{issuer()}/v1/user`。
- **MEDIUM xAI Official: PASS（成立）**。`cf-plugin-marketplace/src/lib.rs:26` 实测 `pub const OFFICIAL_SOURCE_NAME: &str = "xAI Official";` 未改。
- **MEDIUM qidi: 前缀： PASS（成立）**。`cf-pager/src/app/cli.rs:299` 与 `:305` 两处 `eprintln!("qidi: --plugin-dir …")` 仍为 `qidi:`。
- **仲裁合理性（HIGH→MEDIUM）: 同意**。`scope_key = {issuer}::{client_id}`（`auth.rs:53-55`），跨 scope 兜底 token 发往**当前** issuer 的 `/v1/user`；异 issuer token 必被服务端 401/403 拒绝，且 `:177-196` 已兜底为"凭证失效"detail + 本地暂显旧 email/user_id。无服务端越权通路，实际影响上限为"本地显示错误账号信息/刷新失败"，T2 的 HIGH（越权）定级过重，指挥官降 MEDIUM 合理。

### 漏检扫描
- `remote/client.rs`：纯域名品牌替换（均 https、自有域），含 `share_url` 无逻辑变更，无 URL 校验绕过；测试数据残留 `"owned_by":"xai"` 属已知品牌残留类。
- `cf-shell/src/auth/`（oidc/protocol、device_code、external_auth、storage、error、config）：diff 全为 `auth.x.ai→api.qidiai.ltd`、`referrer cf-tools→qidi-code`、文案汉化，无注入/并发/逻辑变更。
- `cf-update/auto_update.rs`：品牌+i18n 替换；`restart_grok` `env_clear` 为存量逻辑；`--pattern 'grok-*'` 残留归入已知品牌残留；签名/哈希强校验路径未触碰。
- `useAuth.ts:2` 注释+代码确认 token 不出 Rust 层（`:9/:14/:16` 的 `tokens*` 仅为用量计数）。
- **未发现新问题。**

### 终审判决
**FINAL VERDICT: REJECT**

维持指挥官仲裁：CRITICAL（20 个乱码文件仍含 6 个真实密钥×5 份转储、测试每次运行再生）与 HIGH（8 个孤儿快照=8 个测试当前必败）均经独立实点复核成立，构成合并阻断；返工清单第 1、2 项（删文件+轮换密钥+修 hooks.rs 测试写文件方式+`.gitignore`；快照迁移/重录）为放行前置条件，MEDIUM 三项（auth.rs 唯一性校验、`OFFICIAL_SOURCE_NAME`、`qidi:`→`qidiwork:`）建议同批修复后复审。
