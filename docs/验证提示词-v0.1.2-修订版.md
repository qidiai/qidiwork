任务：QidiWork v0.1.2 出厂前验证与打包准备（修订版 rev2）

仓库：G:\qidiwork（Windows，Git Bash）。基线：main 分支 <提交后填写——先执行 R3 功能提交，取新 HEAD 填入>，
先跑 `git status` 确认工作树干净且 HEAD 一致，不一致立即停止上报，禁止 checkout/reset。

总纪律（违反即停）：
1. 禁止 git push、禁止发布、禁止改远程服务器；所有产出只到「待人工验收」状态。
2. 任何可能超过 10 分钟的编译，先报告预计耗时等确认再跑。
3. 不许动用户真实的 ~/.qidi 目录；隔离环境一律 QIDI_HOME=<系统临时目录>/qidi-012-test。
4. 报告事实必须可验证：贴命令与关键输出，不许凭猜测下结论。
   exe 陈旧陷阱：报告「程序行为」前对比源码与二进制 mtime，用 target 下最新产物，别用 PATH 旧版本。
5. 【新】全程构建强制 `CARGO_TARGET_DIR=C:\rust-target-qidiwork`（SSD）。G 盘为 5400 转 HDD，
   其上链接大型测试二进制实测 2.5 小时未完成；SSD 上 debug 全量约 25 分钟（debug 缓存已清，
   release 缓存仍在可复用）。不用该环境变量的构建一律不得开始。
6. 【新】GUI 冒烟涉及原生窗口交互：执行者需具备 Windows 桌面截图/操作能力（无此能力则
   该步转人工辅助，逐项记录），截图统一输出到 <临时目录>/qidi-012-shots/ 并在报告中列出。

步骤〇【新】：单元测试门禁（先于一切，快速筛回归）
  cargo test -p qidiwork-gui -p cf-plugin-marketplace
  （本批含 46 行 acp_bridge 新测试；失败则停，贴失败用例。）

步骤一：GUI 冒烟（调试构建）
  export CARGO_TARGET_DIR=/c/rust-target-qidiwork
  cd /g/qidiwork/gui && npm install --no-audit --no-fund && npm run tauri dev
  【修订】首跑含新依赖（tauri-plugin-dialog）+ debug 全量：SSD 预计 20~40 分钟（原估 5~15 偏乐观），报耗时等确认。
  窗口起来后逐项验证并截图：
  a. 「+ 新建任务」旁文件夹按钮 → 系统目录选择器；选定后系统提示含「工作目录」；
     再点「+ 新建任务」复用该目录（localStorage qidi.lastCwd）。
  b. 未连接内核时点「+ 新建任务」不报错卡死（自动拉起会话）。
  c. 左栏折叠 44px 图标条（展开/新建/选目录三键），刷新后状态保持。
  d. 右面板折叠竖排「产物」窄条；折叠期间产生产物亮红点，展开消失。
  e. 右面板左缘拖拽调宽（280~560px），刷新后保持。
  f. 底部状态栏模型下拉：切换后聊天区「模型已切换为…」，后续回合按新模型跑（mock 内核 A/B 模型即可验）；
     切换失败错误进转录；旧内核无清单时降级纯显示。
  g. 回归：权限弹窗、产物预览（docx/xlsx/pdf/md）、任务切换、历史会话恢复不劣化。
  h.【新·若 R2 已实现】设置面板「导出诊断日志」→ 桌面出现 zip → 解开含 GUI 日志/unified 尾段/
     版本信息，抽检无明文密钥（redact 生效）。
  任一失败：记录复现步骤 + devtools 控制台报错，不要自己改代码。

步骤二：内核 git 克隆修复验证（0.1.1 出厂 bug，必须过）
  1) export CARGO_TARGET_DIR=/c/rust-target-qidiwork
     cargo build --release -p cf-pager-bin   （release 缓存在，预计 5~15 分钟增量）
     产物 target/release/qidiwork.exe，版本信息含当前 commit 与内核版本号。
  2) QIDI_HOME 隔离环境验证：
     qidiwork plugin marketplace add https://github.com/qidiai/plugin-marketplace.git
     qidiwork plugin list --json --available
     验收：无 "there is no TLS stream available"、无 "unknown option 'no-hooks'"；能看到 qidi-guide 插件。
  【新】网络判定规则：本机到 github.com 间歇性被重置（GFW）。失败时重试 3 次（间隔 10s）；
     若报 Connection was reset / timed out 且无 git 错误语义 → 标注「网络环境失败，非代码回归」，
     再失败改由服务器侧代克隆到临时目录后本地 file:// 验证（只读，不动远程）。

步骤二·五【新】：更新通知链路验证（R1 客户端侧）
  1) 检查内核版本号已 bump（R1.1：cf-update/cf-pager-bin 应为 0.1.220-alpha.5），
     `qidiwork --version` 输出对齐。
  2) GUI 新版本提示条：用本地临时静态清单（或 hosts/代理指向测试清单）把 latest.gui.version
     置为 0.1.3 → 启动 dev GUI 应出现「新版本可用」提示并链到官网下载页；
     清单版本 ≤ 自身时提示消失。（发布后线上复验一步归发布流程，不在本验证内。）

步骤三：版本号与打包（做完〇一二·五且人工确认后才做）
  【修订】版本号同步全量清单（先搜证再改）：
  - gui/package.json（0.1.2）+ gui/src-tauri/Cargo.toml（0.1.2）——package-lock 由 npm 顺带
  - 内核：cf-pager-bin / cf-update → 0.1.220-alpha.5（R1.1）
  - tauri.conf.json 无 version 字段（已核实），不在清单内；nsis 安装包名随 productName+版本自动
  改完跑 npx vue-tsc --noEmit 与 cargo check -p qidiwork-gui -p cf-pager-bin，再等发布指令。
  NSIS 打包命令执行前必须再问。打包产物按惯例复制到 prod/releases/（qidiwork-setup-0.1.2.exe + SHA-256）。

输出：每步一项「命令 / 实际输出关键行 / 通过与否」清单，最后汇总哪些项阻碍发布。
本提示词与仓库 docs/ 规范冲突时以仓库规范为准并上报差异。

——修订记录 rev2（相对原版）：
① 基线哈希留填 + 明确「先提交 R3 再验证」顺序；② 强制 SSD target（纪律 5）；
③ 预估 5-15min 改 20-40min 并注明原因；④ 新增桌面能力假设与截图目录（纪律 6）；
⑤ 新增步骤〇单测门禁；⑥ 步骤二加 GFW 网络判定与重试规则；⑦ 新增步骤二·五更新链路验证；
⑧ 步骤一加 h（诊断日志导出冒烟，挂 R2）；⑨ 步骤三版本清单纠正（tauri.conf.json 剔除、内核版本纳入）。
